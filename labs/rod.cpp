#include <iostream>
#include <stack>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    stack<string> s1;
    stack<string> s2;

    string s;

    cin >> s;

    for (int i = 0; i < s.length(); i++) {
        string

    }

}